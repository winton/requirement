{
  "name": "requirement",
  "version": "0.0.8",
  "description": "Generate project index files to simplify requiring dependencies",
  "main": "index.js",
  "scripts": {
    "pretest": "npm run compile",
    "prepublish": "npm run compile",
    "compile": "coffee --compile --output lib/ src/",
    "start": "coffee -w --compile --output lib/ src/",
    "test": "jasmine"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:winton/requirement.git"
  },
  "author": "Winton Welsh <winton@cyclo.ps>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/winton/requirement/issues"
  },
  "homepage": "https://github.com/winton/requirement",
  "dependencies": {
    "coffee-script": "^1.9.3",
    "js2coffee": "^2.1.0",
    "lodash": "^3.10.1"
  },
  "devDependencies": {
    "jasmine": ""
  }
}
